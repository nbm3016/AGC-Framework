# Status

Public lock: `core/` verifier, frozen baseline, 14D constitution.

Ambient: \(Y^{14} \to M^4\). \(\Sigma^5\) and \(T^{1,1}\) are factors.

`continuous_knobs = 0`. Verify from `core/` with `python AGC_Computational_Framework.py --full` and without `--regenerate`. Expected result: 8/8 PASS.

A is the 7-dimensional isometry of the locked shape. B is the 8-dimensional holonomy look of the cone. The eight-gluon name is a reading of B, not an A-lock of unique color. \(9/16\) is not \(G_N\). \(\theta_{13}\) stays \(7.953^\circ\).

Module A is a wall-index rule. \(N_{\mathrm{gen}}=3\) names its three zeros on \(j_2=r=0\): \((1/2,0,0)\), \((1,0,0)\), \((3/2,0,0)\), with first-\(\mathrm{SU}(2)\) dimensions 2, 3, and 4. One doublet, one triplet, one quartet. Not three copies of one 4D Weyl spinor. “Three generations” is a name, not an A-lock. The count is unchanged. Road 1: leftovers still differ, so new sentences call the integer `wall_zero_count = 3`. Road 1 catalog: three wall fields \(2\oplus 3\oplus 4\) of \(\mathrm{SU}(2)_1\). Not three generations. 4D Lorentz spin of each is not forced. The rule is not \(\ker D_5\) and not the APS index theorem. The public solver comment that says "APS index theorem" is a label, not the theorem.

Public repository `nbm3016/AGC-Framework` is `core/` and `docs/`. The private repository holds the rest. This note does not move either tree.
